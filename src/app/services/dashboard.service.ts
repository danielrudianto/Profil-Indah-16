import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root',
})
export class DashboardService {
  constructor() {
    // TODO
    // 1. Get last synced data
    // 2. If last synced is more than 10 minutes ago, get fresh data
  }
}
